PROGRAM = button_example

BUTTON1_GPIO ?= 4
BUTTON2_GPIO ?= 5

EXTRA_CFLAGS += -DBUTTON1_GPIO=$(BUTTON1_GPIO) -DBUTTON2_GPIO=$(BUTTON2_GPIO)

EXTRA_COMPONENTS = $(abspath ..)

include $(SDK_PATH)/common.mk

monitor:
	$(FILTEROUTPUT) --port $(ESPPORT) --baud 115200 --elf $(PROGRAM_OUT)
